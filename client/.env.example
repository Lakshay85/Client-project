# ==============================================================================
# FormEnclave Client Environment Configuration
# ==============================================================================
# Mode: "development" (local backend) or "production" (cloud backend)
VITE_APP_ENV=development

# Explicit API URL (leave set to switch immediately, or comment out to use VITE_APP_ENV)
VITE_API_URL=http://localhost:4000

# Backend Endpoints Reference:
VITE_API_URL_DEV=http://localhost:4000
VITE_API_URL_PROD=https://form-enclave-qg7w.onrender.com
